# Ralph Progress Log
Started: 2024-01-15

## Codebase Patterns
- Migrations: IF NOT EXISTS
- Types: Export from actions.ts
- **Tests: Import from TS source files, NOT compiled JS** (avoid runtime source not found errors)
- **Chair Context Mock**: Use `app.mockContext()` for testing
- **Coverage Check**: Run `tnpm run test -- --coverage` after each implementation
- **Quest Mode**: When coverage doesn't meet target, use quest to generate targeted test cases

## Key Files
- db/schema.ts
- app/auth/actions.ts
- test/ (all test files)
---

## 2024-01-15 - US-001
- What was implemented: Added login form with email/password fields
- Files changed: app/auth/login.tsx, app/auth/actions.ts
- **Learnings:**
  - Patterns discovered: Use IF NOT EXISTS for migrations
  - Gotchas encountered: Need to handle email validation on both client and server
---

## 2026-02-21 - Fecodex Service 层测试覆盖
- What was implemented: Asset.ts 和 Idea2CodeChat.ts 服务层单元测试
- Files created:
  - test/unit/modules/fecodex/service/Asset.test.ts (31 测试用例)
  - test/unit/modules/fecodex/service/Idea2CodeChat.test.ts (34 测试用例)
- **Learnings:**
  - Patterns discovered: 
    - 使用 `ctx.getModule('fecodex').serviceName` 获取模块服务
    - Mock 外部依赖时注意链式调用 (如 `mock(service.bone, 'transaction', async (fn) => fn({}))`)
    - 测试描述需包含行号引用以便追踪覆盖率
  - Gotchas encountered:
    - Tegg 依赖注入的服务需要通过 module 获取
    - Transaction Mock 需要正确传递回调函数
---

## 2026-02-21 - Fecodex Controller 层集成测试
- What was implemented: Dashboard Asset Controller 集成测试
- Files created:
  - test/integration/modules/fecodex/controller/Asset.test.ts (35 测试用例)
- **Learnings:**
  - Patterns discovered:
    - 使用 `create('model_name', {...})` Factory 创建测试数据
    - 使用 `mockUser(user)` 模拟登录用户
    - HTTP 测试使用 `request.get/post/delete()` 全局方法
  - Gotchas encountered:
    - 需要先创建关联数据 (user, library) 再创建资产
    - 权限测试需要创建不同用户来验证
---
