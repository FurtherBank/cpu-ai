# Subagent 单点派发契约

```markdown
## 用户原始需求

${原文，不摘要}

## 单一承重内容单元

${只写一个精确待确认单元}

## 硬依赖输入

${文件路径、ID、必要原文}

## 目标与完成判定

${状态变化、交付物、通过条件、禁止事项}

## 方法论依据

${指导文件绝对路径或相对 Skill 路径}

## 输出

用 apply_patch 写入：${绝对路径}

最终返回：交付物路径、结论、证据锚点、淘汰备选、边界、未知、≤150 字交接摘要、auditSummary；显式子 Skill 调用另返 behaviorLogPath。
```
