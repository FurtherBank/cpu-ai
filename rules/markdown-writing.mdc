---
description: Markdown 载体格式规范：标题禁止编号、流程图默认 Mermaid、图片占位声明
globs: **/*.md
alwaysApply: false
---

# Markdown 载体格式规范

只管 Markdown 这个载体的格式约定。写作内容层面的规则（分档、面向读者的表达、落笔自查、造词契约、落盘关口）见 writing-expression 规则。

## 标题编号

**默认禁止**对任何级别的标题使用编号，包括但不限于：`一、`、`步骤一、`、`1.`、`1.1`、`第一章` 等形式。

例外：仅当不加编号**必然导致读者理解顺序出错**时（如严格依序执行的流程步骤），才可使用编号。

```markdown
❌ 禁止

## 一、背景

## 二、方案设计

### 2.1 核心模块

✅ 正确

## 背景

## 方案设计

### 核心模块
```

## 流程图

需要表达流程、时序、状态或关系图时，**默认使用 Mermaid**（Markdown 中用 ` ```mermaid ` fenced 代码块），便于版本管理与渲染一致。

````markdown
```mermaid
flowchart LR
  A[开始] --> B[处理]
  B --> C[结束]
```
````

仅当必须用位图/矢量工具才能表达时，再改用图片占位 `[🖼️ …]` 或外链图。

## 图片占位声明

对于**对外分享**的文档，或用户/任务要求中**明确要求图文形式**的文档，在需要配图的位置添加如下格式的占位声明：

```
[🖼️ 图片内容描述]
```

描述应简明说明该图需要表达的内容，如：

```markdown
[🖼️ 系统整体架构图，展示前端、网关、服务层的调用关系]
[🖼️ 操作截图：点击「新建项目」按钮后弹出的配置表单]
```

普通内部文档（非对外、非图文）无需主动配图，不加此占位；仅当确需一张图且 Mermaid 表达不了时，按"流程图"一节降级使用占位或外链图。
