---
description: Markdown 写作：标题禁止编号、面向读者表达、图文占位、流程图默认 Mermaid
globs: **/*.md
alwaysApply: false
---

# Markdown 写作规范

## 标题编号

**默认禁止**对任何级别的标题使用编号，包括但不限于：`一、`、`步骤一、`、`1.`、`1.1`、`第一章` 等形式。

例外：仅当不加编号**必然导致读者理解顺序出错**时（如严格依序执行的流程步骤），才可使用编号。

```markdown
❌ 禁止

## 一、背景

## 二、方案设计

### 2.1 核心模块

✅ 正确

## 背景

## 方案设计

### 核心模块
```

## 面向读者的表达

如果文档后续会作为给其他人阅读或者使用(形式包括但不限于**说明/分享文档**、**Skill**、**代码注释**)，而不只是你或者用户一个人自己留存；

那么你需要时刻注意：文档在**读者视角**消费过程没有问题，会相比只是自己能懂，在表达内容范围上、会有很多额外的约束：

- **先判断阅读任务**：不要只问“读者是谁”，还要问“读者此刻要用这份文档完成什么”。例如，业务方、产品、设计、测试、运维、研发同事、评审人、未来维护者或外部用户都只是常见例子；真正要识别的是他们当前关心的决策、动作、风险和边界。
- **突出能帮助读者完成任务的信息**：围绕读者的使用场景组织内容。需要判断取舍的人更需要目标、范围、影响和风险；需要验收的人更需要验收口径、边界场景和变更点；需要落地的人更需要方案、契约、步骤、监控和回滚。不要让读者看完后还要自行猜测重点。
- **删掉读者用不到的信息**：不要为了展示过程而堆叠思考草稿、局部尝试、临时命令输出、内部讨论细节或与结论无关的代码路径。保留能支撑判断的依据，删除不会改变读者行动的信息。
- **避免暴露不该表达的信息**：面向非研发或外部读者时，不写内部代码字段、数据库表名、密钥、账号、内部接口地址、权限策略细节、未公开的业务规则、人员评价和组织内部信息。确需说明时，用稳定概念替代实现细节，例如写“用户身份校验失败”而不是展开内部 token 字段结构。
- **用读者能理解的层级组织内容**：先交代结论、背景、影响范围和下一步，再补充细节。不要让读者先读大量实现细节后自行归纳重点。
- **措辞保持可验证和可交付**：少用“应该没问题”“大概支持”“尽量优化”等含糊表达；改为说明明确状态、约束、证据和待确认项，例如“已覆盖 A/B 两类场景，C 场景待联调确认”。
- **区分事实、判断和建议**：事实写证据，判断写依据，建议写取舍。不要把个人倾向包装成既定结论，也不要把未验证假设写成已发生事实。
- **避免把内部责任转嫁给读者**：如果文档要求读者行动，写清触发条件、执行动作、预期结果和异常处理；不要只写“注意”“谨慎”“自行确认”等缺少操作路径的提醒。

如有必要，也请在写完文档后，通过单独的**Subagent**说明请完全带入读者身份(说明读者身份特征)，来校验文档在读者视角消费过程没有问题。

## 流程图

需要表达流程、时序、状态或关系图时，**默认使用 Mermaid**（Markdown 中用 ` ```mermaid ` fenced 代码块），便于版本管理与渲染一致。

````markdown
```mermaid
flowchart LR
  A[开始] --> B[处理]
  B --> C[结束]
```
````

仅当必须用位图/矢量工具才能表达时，再改用图片占位 `[🖼️ …]` 或外链图。

## 图片素材占位声明

对于**对外分享**或**明确声明为图文类型**的文档，在需要配图的位置添加如下格式的占位声明：

```
[🖼️ 图片内容描述]
```

描述应简明说明该图需要表达的内容，如：

```markdown
[🖼️ 系统整体架构图，展示前端、网关、服务层的调用关系]
[🖼️ 操作截图：点击「新建项目」按钮后弹出的配置表单]
```

普通内部文档（非对外、非图文）无需添加此占位。
